"""
Shadow Files command execution layer.

The command layer receives validated intents and controls access to
actual application operations.

Commands must not bypass authorization, validation, state controls,
or audit requirements.
"""
